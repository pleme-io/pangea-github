# pangea-github

> **★★★ CSE / Knowable Construction.** This repo operates under **Constructive Substrate Engineering** — canonical specification at [`pleme-io/theory/CONSTRUCTIVE-SUBSTRATE-ENGINEERING.md`](https://github.com/pleme-io/theory/blob/main/CONSTRUCTIVE-SUBSTRATE-ENGINEERING.md). The Compounding Directive (operational rules: solve once, load-bearing fixes only, idiom-first, models stay current, direction beats velocity) is in the org-level pleme-io/CLAUDE.md ★★★ section. Read both before non-trivial changes.


<!-- Blackmatter alignment: pillars 5, 12 -->
<!-- See ~/code/github/pleme-io/BLACKMATTER.md for pillar definitions. -->

## Blackmatter pillars upheld

- **Pillar 5** (Infrastructure declaration): GitHub resources as typed Pangea functions — org membership, repo settings, branch protection, labels, environments rendered through the same Terraform pipeline as AWS/GCP/Azure.
- **Pillar 12** (Generation over composition): the 85 resources are auto-generated by `terraform-schema-importer` from the Integrations GitHub provider schema. Never hand-edit; regenerate from the schema.

Auto-generated typed GitHub Terraform resource functions for the Pangea IaC DSL.
All resource code is generated by `terraform-schema-importer` from the
Integrations GitHub Terraform provider schema.

## Generation

```bash
terraform-schema-importer generate \
  --schema schema.json \
  --provider "registry.terraform.io/integrations/github" \
  --provider-name github \
  --output-dir .
```

## Resources: 85

All resources use the `define_resource` DSL with `Dry::Struct` type validation.
Module: `Pangea::Resources::Github`, prefix: `github_`.

## Regeneration

1. Update the provider version in a Terraform workspace
2. Run `terraform init && terraform providers schema -json > schema.json`
3. Run the generate command above
4. Run `bundle exec rspec` to validate
5. Commit the updated files

## Dependencies

pangea-core ~> 0.2, terraform-synthesizer ~> 0.0.28, dry-types ~> 1.7,
dry-struct ~> 1.6. Ruby >= 3.3.
